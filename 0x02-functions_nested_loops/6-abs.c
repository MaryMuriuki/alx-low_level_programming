#include "main.h"
/**
 * _abs(int) - computes the absolute value of an integer
 * @k:  integer being checked
 * Return: -k or k if successful
 */
int _abs(int k)
{
	if (k >= 0)
	{
		return (k);
	}
	return (-k);
}
