#include "main.h"
/**
 * _isdigit - A func that checks for a digit0 thro' 9
 * @c: the digit being checked
 * Return: 1 if c is a digit, 0 otherwise
 */
int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}
